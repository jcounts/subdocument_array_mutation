Run as:

python subdocument_array_mutation.py <document> <mutation>

where "document" is the name of the JSON document to find changes for and "mutation"
is the name of the JSON document containing mutation instructions.

Example:

python subdocument_array_mutation.py example_doc.json example_mutation.json
